---
response_to: KR-20260714-004
created: 2026-07-14
from: machine/nt8lab
reviewer: Fable-5 (operator-mediated)
status: answered
sensitivity: repository-safe
related_dimension: 3d-printing
---

# Response — Notebook Object-Capture Kit Review

Proposal quality noted: seed design rules preserved intact, guardrails
added unprompted, scan-to-compare correctly first. The MVP shape
(session folder + manifest + log + mechanical checks, no pixel
invention) is approved in principle, contingent on the decisions below.

## D1 — Responsibility boundary: APPROVED as proposed.
Notebook = capture + logging + preflight checks. Desktop (GTX 1080 Ti;
CUDA available, so COLMAP GPU dense is on the menu) = reconstruction,
measurement, banking. Binaries out-of-band; git carries repository-safe
records only.

## D2 — Transfer root & retention: PENDING-OPERATOR, recommendation:
Root: desktop `_DIMENSIONS/3d-printing/artifacts/incoming/<session_id>/`.
Retention: raw frames kept until the session's reconstruction or
measurement result is banked; thereafter operator's discretion per
session. Transfer medium for trial one: existing network (Wi-Fi) is
acceptable; a direct wired link is booked as an upgrade whose trigger is
measured annoyance, not anticipation.

D2 CONFIRMED — transfer root
`_DIMENSIONS/3d-printing/artifacts/incoming/<session_id>/`, Wi-Fi
acceptable for trial one.

## D3 — First calibration object: PENDING-OPERATOR (physical).
Recommendation: an existing printed calibration cube. REQUIREMENT: the
known measurement must be operator-caliper-measured on the physical
object and recorded in the capture log before reconstruction —
as-printed actual, not as-designed nominal. The nominal model dimension
is recorded separately; the delta between nominal, caliper, and
scan-measured is the whole point of the compare loop.

D3 AMENDED — no caliper on hand; trial one downgraded to
RECONSTRUCTION-FEASIBILITY (rig: object on stick with dice as scale
reference, manual stop-spin-shoot, two orientations, plain background,
stills only); ruler ground truth ±0.5mm recorded as coarse;
DIMENSIONAL-ACCURACY becomes trial two, gated on caliper acquisition.
Backend decision: Python + OpenCV for checks; v1 ingests Windows Camera
app photos via watcher; direct camera control and GUI are later
iterations.

## D4 — Camera control: RULED — ingest first, control later.
MVP ingests photographs saved by the Windows Camera app (or phone
transfer) into the session folder. Direct camera automation is a later
upgrade, built only if ingestion friction demonstrably hurts. No camera
libraries, no new trust surface in v1.

## D5 — Metadata split: RULED.
Repository-safe (may appear in logs/manifests that travel git): session
id, object name, purpose, camera TYPE (webcam/phone), frame count,
per-frame content hashes, lighting description, known-dimension values,
timestamps, check results. Local-only (stays beside binaries, never
git): absolute paths, share/host/IP details, and ALL EXIF — the
manifest/check step must strip or refuse to copy EXIF into any
repository-bound file (phone EXIF embeds GPS). Treat EXIF handling as a
mechanical check with a pass/fail line in checks/.

## D6 — scan_pipeline.py: RULED — verify-or-declare before handoff.
The desktop side must locate the authoritative script, or declare it
absent and bank a recovered/authored version, BEFORE the first frame
handoff. No handoff proceeds against a dangling reference (third
ghost-reference pattern this week; the rule stands: verify present,
then proceed).

## Guardrails: all four ENDORSED as written.
Shutter open only during active sessions; no mic; no AI
super-resolution/generative fill in measurement captures; no installers
or automation authorized by this response either — the MVP build gets
its own explicit go after D2/D3 are filled by the operator.

## Sequence from here
1. Operator supplies D2 root confirmation + D3 object and caliper value.
2. Desktop resolves D6 (locate or bank scan_pipeline.py).
3. MVP build authorization issues as a separate go.
4. Four-trial baseline runs; stacking survives only on measured merit.
