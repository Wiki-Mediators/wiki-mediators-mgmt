---
response_to: KR-20260715-006
created: 2026-07-15
from: machine/nt8lab
target: machine/ryry
status: answered
sensitivity: repository-safe
related_request: KR-20260714-005
---

# Desktop intake ready — retained reconstruction set verified

The handoff was read from `origin/machine/ryry` at
`d36c7dfff41fa328d2beffc9ba51dd36d6c65e71` without merging either machine
branch into `main`.

## Intake verification

- The removable FAT32 volume and exact package/payload folder were present.
- The desktop destination did not exist before initial intake, so no prior
  payload was overwritten.
- Windows initially lost the USB during the copy at `images/F0050.jpg`
  (`ERROR 433`, then `ERROR 21: device is not ready`). After the operator
  replugged it, the non-deleting copy resumed successfully.
- The operator amended retention to keep the EXIF-free reconstruction set and
  its session evidence, but not the duplicate `originals/` provenance set.
- The retained desktop set contains 261 files and 296,527,643 bytes: 260 files
  covered by `TRANSFER_SHA256.csv` plus the transfer manifest itself.
- Verification found 0 missing files, 0 size mismatches, and 0 SHA-256
  mismatches. The copied transfer manifest is byte-identical to the USB copy.
- The retained structure contains 128 files under `images/`, 129 files under
  `checks/`, the session/manifest/README files, and 0 files under `originals/`.
- No reconstruction was started.

## D6 pipeline resolution

The authoritative pipeline is banked at
`_DIMENSIONS/3d-printing/artifacts/scan_pipeline.py`, SHA-256
`6C593E02762856B9EDF36EAA9879BF3F15C908A77C9CFC09F1F7482E33AAE90A`.
The canonical Downloads working copy is byte-identical. The banked script
contains the current `CENTER_CROP`, `REMOVE_GROUND`, `KEEP_LARGEST_ONLY`, PLY
passthrough, and triangle-normal export behavior.

Reconstruction input is confirmed as the payload's 128 EXIF-free files under
`images/`, never `originals/`.

## Readiness

READY for the reconstruction-feasibility baseline using the retained
`images/` folder. The omitted `originals/` files remain on the USB and are not
needed for reconstruction. The next action may run the authoritative banked
pipeline; this response does not claim that reconstruction has run yet.
