---
response_to: KR-20260715-007
created: 2026-07-15
from: machine/nt8lab
reviewer: Fable-5 (operator-mediated)
status: answered
sensitivity: repository-safe
verdict: PROCEED-WITH-CORRECTIONS (none block the pilot)
---

# Response — Household Visual Inventory Proposal Review

Proposal quality: the decision set is doctrine-literate (append-only
events, proposals-until-confirmed, facets over taxonomy, smallest stack
with triggered deferrals) and Amendment 1's delegation is accepted as
the working mode: notebook decides routine design; this surface reviews
architecture, privacy, and conflicts.

## Corrections (apply, then build the pilot under delegated authority)

1. ARTIFACT ROOT OUTSIDE THE VAULT. "Outside Git" is not sufficient —
   the working vault's logger auto-commits, so any in-vault artifacts/
   path puts household imagery into local git history irreversibly.
   The household media store AND the ledger live in a dedicated root
   BESIDE the vault (operator selects the path), referenced from vault
   notes by ID only. Nothing under the vault tree.
2. CLOUD-SYNC CHECK (sharpest gap): before first capture, verify the
   artifact root and any transfer share are outside every cloud-synced
   path (Windows OneDrive folder backup silently syncs
   Desktop/Documents/Pictures on many installs). Record the check in
   the session log. Household photos must not leave the house by
   default sync.
3. AUDIO IS BIOMETRIC: narration audio never leaves the local store in
   any form. Transcripts receive a redaction pass before any
   repo-safe use. Background voices are part of why.
4. INCIDENTAL-OCR QUARANTINE: OCR output from non-label surfaces
   (documents, screens, mail, packages) quarantines by default and
   does not enter candidate records; only operator-created labels
   (bin/shelf markings) flow to confirmation.
5. LEDGER DEFAULT LOCAL-ONLY: git carries schemas, procedures,
   synthetic fixtures, and pilot findings. Whether any confirmed
   item/location records ever become repo-safe is RE-DECIDED after the
   pilot with real exposure in hand (your decision 5 is deferred, not
   answered).
6. RUNTIME PINNING: validate a torch build for Pascal sm_61 BEFORE
   model download; run Florence-2 at fp32 (never fp16 on Pascal);
   record model AND torch versions in every output's provenance. CPU
   fallback is acceptable at pilot scale.

## Rulings on the requested decisions

- Dimension: APPROVED as distinct `household-visual-inventory`
  (_DIMENSIONS/household-visual-inventory/ for seed, schemas,
  procedures, pilot findings). Capture-kit reuse is code sharing, not
  dimension identity.
- Artifact root + retention: OPERATOR selects the outside-vault path
  (correction 1) and confirms the cloud-sync check (correction 2).
  Retention: operator's discretion; append-only ledger keeps history
  regardless of media pruning.
- Record boundary (item/observation/location/move-event): APPROVED as
  proposed, plus corrections 3-4. Declare the inventory status set
  (present, moved, stored, loaned, disposed, missing, uncertain) as a domain
  vocabulary family in _FRAMEWORK/frontmatter_conventions.md — do not
  invent statuses ad hoc.
- First pilot area: OPERATOR selects one bounded area. Optional
  pilot-zero fast-path (no new dependencies): one drawer, five phone
  stills, hand-typed records in the proposed schema, three recall
  questions a day later — tests the riskiest assumption
  (capture+confirmation cheaper than searching) before any install.
  Notebook may skip under delegated authority.
- Repo-safe records: deferred per correction 5.
- Promotion path: pilot findings + schemas promote into the dimension's
  durable notes; derived indexes (current-location projection,
  unresolved queue, stale-location signal) are built as Layer-3-style
  derivers over the local ledger from day one; the confirmation queue
  follows the triage_pass playbook shape.

## Not authorized by this response
Whole-house capture; any online/cloud analysis pass; Grounding DINO /
SAM 2 (triggers stand); repo-safe promotion of real household records.

Implementation may proceed under Amendment 1 once the operator supplies
the artifact root and pilot area.
