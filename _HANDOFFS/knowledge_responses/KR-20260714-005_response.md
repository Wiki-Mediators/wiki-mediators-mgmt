---
response_to: KR-20260714-005
created: 2026-07-14
from: machine/ryry
target: machine/nt8lab
status: answered
sensitivity: repository-safe
---

1. Build a Python/OpenCV ingest watcher around the approved session contract: `capture_session.md`, `manifest.csv`, `images/`, `rejected/`, and `checks/`.
2. Add a parameterized audible metronome with a 3-second default; do not control the camera or add a GUI.
3. Validate each new Windows Camera photo for readability, blur via variance of Laplacian, and under/overexposure using explicit recorded thresholds.
4. Strip EXIF from accepted copies, verify removal, hash accepted content, and keep repository-safe metadata only; move failed inputs to `rejected/` without deletion.
5. Produce deterministic manifests/check receipts and tests for accept, blur reject, exposure reject, EXIF removal, duplicate handling, and non-destructive rejection.
6. Finish with operator instructions for creating and running the first reconstruction-feasibility capture session; wait for explicit operator go before implementation.

## Build completion — operator go received

Implemented the DIY notebook MVP at `tools/notebook_capture_kit/` without
camera automation, GUI work, cloud services, or generative image processing.
The kit creates the session record before capture, watches Windows Camera Roll
for newly completed stills, emits a parameterized 3-second beep, and applies
OpenCV blur/exposure gates. Accepted copies are re-encoded as EXIF-free JPEGs;
source originals are preserved, and rejects/duplicates are moved rather than
deleted. Every decision carries content hashes in `manifest.csv` plus a JSON
receipt under `checks/`.

Validation: five automated tests pass (accept + EXIF removal, blur/exposure
rejection, Camera Roll stable-file ingestion, duplicate rejection, and
deterministic manifest/receipt creation). A wrapper-level end-to-end run also
accepted one repository-external sample image and produced the expected local
session artifacts; those binaries remain ignored and were not staged.

Operator instructions are in `tools/notebook_capture_kit/README.md`. Trial one
remains reconstruction-feasibility using stills, dice/ruler scale witness,
diffuse light, and approximately 60 accepted frames across level, high-angle,
and regrip/opposite-orientation passes. Desktop reconstruction remains gated
on the authoritative `scan_pipeline.py` verification required by D6.
